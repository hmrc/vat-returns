# microservice specific routes

GET     /returns/vrn/:vrn    @controllers.VatReturnsController.getVatReturns(vrn: String, filters: models.VatReturnFilters)
POST    /returns/vrn/:vrn    @controllers.SubmitVatReturnController.submitVatReturn(vrn: String)

POST    /nrs/submission          @controllers.NRSController.submitNRS(vrn: String)
